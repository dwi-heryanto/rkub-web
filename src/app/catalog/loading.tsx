import { Card } from "@/components/ui/card";

export default function CatalogLoading() {
  return (
    <Card className="text-sm text-[--color-text-muted]">
      Loading catalog...
    </Card>
  );
}
